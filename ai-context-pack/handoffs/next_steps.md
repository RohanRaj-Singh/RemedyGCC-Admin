# Next Steps

## High-Value Follow-Ups
- Verify the desktop preview column alignment on real browser widths and fine-tune fixed positioning if needed
- Apply the same split branding-editor pattern to `src/app/tenants/new/page.tsx` if create and edit pages should stay consistent
- Add visual regression or interaction coverage for tenant branding updates
- Replace mock tenant/scanner services with real backend integration when API endpoints are ready

## Tenant UX Follow-Ups
- Consider adding a small collapsed/mobile preview state for narrower screens
- Consider adding safe preset suggestions without replacing the simplified custom controls
- Consider a lightweight accessibility guard for low-contrast text/background combinations

## Documentation Follow-Ups
- Keep AI context files in sync with actual UI behavior
- Update module/rules docs whenever tenant branding flow or layout patterns change
