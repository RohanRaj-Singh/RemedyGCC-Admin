# Attribute Edge Cases

This edge-case reference is now tracked in:

- `docs/testing/attribute-flow-edge-cases.md`

## Canonical Rule

All attribute edge-case handling must preserve the strict hierarchy:

`Stream -> Location -> Function -> Department`
